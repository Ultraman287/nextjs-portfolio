/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "study.com",
      "github.com",
      "example.com",
      "blog.appseed.us",
      "datascientest.com",
    ],
  },
};

export default nextConfig;
